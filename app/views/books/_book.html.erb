    <h2>User info</h2>
     <p><%= image_tag user.get_profile_image(100,100) %></p>
      <table class="table">
       <tbody>
        <tr></tr>
        <tr><th>name</th><br>
            <th><%= user.name %></th><br></tr>
        <tr><th>introduction</th><br>
            <th><%= user.introduction %></th><br></tr>
       </tbody>
      </table>

 <div class="row">
   <%= link_to edit_user_path(user.id) do %>
  <button class="btn btn-outline-secondary btn-block"><i class="fas fa-user-cog"></i></button>
  <% end %>
  </div>
  
<h2 class="mt-3">New book</h2>

<%= form_with model: book do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  
  <%= f.submit 'Create Book',class: 'btn btn-success btn-sm' %>
<% end %>

