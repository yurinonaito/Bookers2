  <h3>User info</h3>

  <p><%= image_tag user.get_profile_image(100,100) %></p>
  <p>name　　　　<%= user.name %></p>
  <p>introduction<%= user.introduction %></p>
  
  <p><%= link_to edit_user_path(user.id) do %></p>
  <button class="btn btn-outline-secondary btn-block"><i class="fas fa-user-cog"></i></button>
  <% end %>

  
<h1>New book</h1>

<%= form_with model: book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  
  <%= f.submit 'Create Book',class: 'btn btn-success btn-sm' %>
<% end %>
